(tests
 (names explain delitmus litmusify config config_failures)
 (preprocess
  (pps ppx_jane))
 (libraries sexplib0 base.caml core_kernel core stdio fmt toplevel fpath
   plumbing act.asm act.c act.common act.config act.delitmus act.litmus
   act.sanitiser act.utils)
 (action
  (run %{test} input))
 (deps
  "input/asm/x86/att/spec"
  (glob_files "input/config/valid/*.conf")
  (glob_files "input/config/failures/*.conf")
  (glob_files "input/litmus/*.litmus")
  (glob_files "input/asm/x86/att/*.s")))
