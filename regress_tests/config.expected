# Config parser (valid) tests

## comments_only.conf

```
()
```
## defaults.conf

```
((Default (Try arch (x86 att)) (Try machine (localhost))
  (Try compiler (localhost gcc x86 normal)) (Try sim (herd))))
```
## empty.conf

```
()
```

Ran 3 test(s).
