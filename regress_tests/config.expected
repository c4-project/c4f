# Config parser (valid) tests

## comments_only.conf

```
()
```
## defaults.conf

```
((Default
  ((Try Arch (x86 att)) (Try Machine (localhost))
   (Try Compiler (localhost gcc x86 normal)) (Try Sim (herd)))))
```
## empty.conf

```
()
```

Ran 3 test(s).
