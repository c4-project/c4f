(library
  (name lang)
  (preprocess (pps ppx_deriving.std ppx_jane ppx_sexp_message))
  (libraries
    core
    utils
    menhirlib
    rresult)
  (modules_without_implementation LangParser))
(ocamllex
  (modules X86ATTLexer lexUtils))
(menhir
  (modules X86ATTParser))
