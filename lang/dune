(library
  (name lang)
  (preprocess (pps ppx_sexp_message))
  (libraries
    core
    menhirlib
    rresult)
  (modules_without_implementation constant pPMode scalar))
(ocamllex
  (modules X86Lexer lexUtils))
(menhir
  (modules X86Parser))
