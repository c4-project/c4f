(executable
  (name main)
  (public_name act)
  (preprocess (pps ppx_jane))
  (libraries
    lib
    x86
    core
    utils
    unix))

(rule
 (with-stdout-to regress.output (run act regress)))

(alias
 (name runtest)
 (action (diff regress.expected regress.output)))