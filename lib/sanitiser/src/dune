;; -*- mode: lisp; -*-

(library
 (name sanitiser)
 (inline_tests)
 (preprocess
  (pps ppx_deriving.std ppx_expect ppx_jane))
 (libraries act_common config fmt fpath abstract litmus utils language
   travesty travesty.core_kernel_exts core shell.string_extended))
