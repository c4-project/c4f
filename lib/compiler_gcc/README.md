# Support for the GNU C compiler and compatibles

This module contains a compiler instance for GCC and compilers that expose a
similar interface (such as Clang).  Said instance permits the use of such
compilers in ACT.
