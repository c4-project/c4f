; -*- mode: lisp; -*-

(library
 (name fuzz_cmd)
 ; This module is deliberately left private, as it's so tightly coupled to the
 ; act binaries that it's not particularly useful on its own.
 (preprocess
  (pps ppx_jane))
 (libraries stdio base base.caml core_kernel core fmt plumbing toplevel act.common act.config act.fuzz act.utils))
