; -*- mode: lisp; -*-

(library
 (name toplevel)
 ; This module is deliberately left private, as it's so tightly coupled to the
 ; act binaries that it's not particularly useful on its own.
 (preprocess
  (pps ppx_jane))
 (libraries yojson sexplib0 stdio base.caml core_kernel core fpath fmt
   fmt.tty unix travesty.base_exts plumbing act.asm
   act.common act.compiler act.compiler_gcc act.config
   act.machine act.sanitiser act.backend act.backend_herd
   act.backend_litmus act.utils act.x86))
