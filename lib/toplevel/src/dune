(library
 (name toplevel)
 ; This module is deliberately left private, as it's so tightly coupled to the
 ; act binaries that it's not particularly useful on its own.
 (inline_tests)
 (preprocess
  (pps ppx_jane))
 (libraries core fmt fmt.tty unix act_c act_config act_sanitiser act_sim
   act_sim_herd act_sim_litmus act_tester act_x86))
