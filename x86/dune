;; -*- mode: lisp; -*-

(library
 (name x86)
 (inline_tests)
 (preprocess
  (pps ppx_deriving.std ppx_expect ppx_jane ppx_sexp_message))
  (libraries
   core
   lib
   utils
   menhirLib))
(ocamllex
 (modules ATTLexer lexUtils))
(menhir
 (modules ATTParser))
